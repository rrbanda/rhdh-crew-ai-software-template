apiVersion: v1
kind: Secret
metadata:
  name: leopard-secrets
type: Opaque
stringData:
  # LlamaStack API Key (if required)
  # Leave empty if LlamaStack doesn't require authentication
  LLAMASTACK_API_KEY: ""
